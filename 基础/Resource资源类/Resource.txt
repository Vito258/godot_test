Resource 继承自RefCounted和Object ，拥有这两者的全部特征
1、当资源文件导入项目中（图片、视频、音效等），会自动生成一个特殊文件和.import文件，import指向特殊文件，特殊文件是资源文件的数据化被godot读取
2、如果加载的文件已经被转化为资源，且此资源引用计数器不为0，则在加载此资源文件时不会创建新的对象，而是返回一个原先对象的引用，如果想要创建新的对象，需要使用类名.new()
3、load和preload的区别，load方法的参数可以跟字符串变量，而preload方法的参数只能是纯字符串
